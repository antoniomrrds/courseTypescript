describe('one test', () => {
  it('should return one', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('Other test', () => {
  it('should return the name Antonio', () => {
    const name = 'Antonio';
    expect(name).toBe('Antonio');
  });
});
